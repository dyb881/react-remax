import React from 'react';
import { View, Text, Image, Input } from 'remax/one';
import styles from './index.css';

export default () => {
  return <View className={styles.app}>react-remax</View>;
};
